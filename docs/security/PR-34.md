# Security review — PR #34: UX polish + content feedback

**Date:** 2026-06-08
**Reviewer:** local security review (Opus) via general-purpose subagent acting in security role.
**Scope:** 11 commits, 58 files — owner content edits (case studies, About, CV, contact, EN+FR), new `_data/case_study_categories.yml` (Liquid-rendered labels/icons), SCSS additions (`ux-polish.scss`, `print.scss`), forked topbar/sidebar (dark-mode toggle relocation, search hidden via CSS), layout FA-icon / category-lookup / back-link / CTA changes, OG banner JPG, deleted tab files, test edits, new `KNOWN_ISSUES.md`.

## (1) What was reviewed

- Category-label Liquid (`case_study_categories[...]` → text node + FA `class` token) for markup break-out.
- `schema_person.yml` / `i18n.yml` edits flowing into JSON-LD / meta — escaping intact.
- Email/phone privacy invariant after the contact + CV rewrites.
- `rel="noopener noreferrer"` on every `target="_blank"` external link.
- Search hidden via CSS (DOM kept) — no secret exposed.
- The CV `onclick="window.print()"` inline handler (static literal).
- `_config.yml` (OG banner path, exclude list), new banner JPG.

## (2) Security-relevant decisions

- Search affordance hidden via CSS with the DOM intact so the theme's search JS initialises cleanly; the index covers only the (empty) `_posts`, so nothing private is exposed. Re-enable is a future feature (see `KNOWN_ISSUES.md`).
- Dark-mode toggle relocated to the topbar keeping `id="mode-toggle"`; theme JS binds by id, unchanged.
- Category labels/icons come from a committed owner data file, rendered at build time — no untrusted-input boundary.

## (3) Findings

No CRITICAL, HIGH, MEDIUM or LOW findings.

## (4) Risks accepted with rationale

| Tag | Severity | Risk | Rationale |
|---|---|---|---|
| — | — | None | Owner-authored content + CSS/UX branch on a static, no-server, no-untrusted-input site; every interpolation is either jsonify-escaped, a text node, or a bare owner token |

## (5) Recommended follow-ups

- None blocking. When search is eventually built, review the new `search.json` index for any field that shouldn't be public.

## Severity roll-up

| CRITICAL | HIGH | MEDIUM | LOW | NONE |
| -------- | ---- | ------ | --- | ---- |
| 0        | 0    | 0      | 0   | —    |
