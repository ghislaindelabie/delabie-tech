# Security review — PR #43: sweep findings (sitemap lastmod fallback + privacy-regex hardening)

**Date:** 2026-06-12
**Reviewer:** lead-reviewer reasoning (budget mode; precedent PR-36/37/40/41). Finding by a single gap-sweep agent; fix applied and probe-proven by the lead reviewer.
**Scope:** 2 lines of code — `sitemap.xml` (`item.data.last_modified_at` → `item.last_modified_at`; the `.data.` form is always empty by Jekyll key promotion, so the fallback was dead) and `tests/structural/email_privacy_spec.rb` (digit-boundary lookarounds on the FR-phone pattern) + review-report execution state.

## Security assessment

- **Privacy posture strengthened:** the lookarounds eliminate a false-positive class (digit runs in minified JS/sourcemaps) without weakening detection — all real phone formats verified still matching, embedded runs verified not matching.
- The sitemap change only restores an intended freshness-signal fallback for future content; no new interpolation, no exposure (lastmod values come from owner frontmatter through `date_to_xmlschema`).
- No secrets, no PII, no workflow changes.

## Findings

No CRITICAL, HIGH, MEDIUM or LOW security findings.

## Severity roll-up

| CRITICAL | HIGH | MEDIUM | LOW | NONE |
| -------- | ---- | ------ | --- | ---- |
| 0        | 0    | 0      | 0   | —    |
