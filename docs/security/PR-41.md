# Security review — PR #41: code review batches B + C

**Date:** 2026-06-10
**Reviewer:** lead-reviewer reasoning (budget mode; precedent PR-36/37/40). Implementation by Opus subagents under TDD; independently verified by the lead reviewer (diff reads, full suites, built-output spot checks).
**Scope:** template/data guards (FR-permalink rule via shared lib + spec + CLI, lang-switcher Liquid fallback, teaching `source_url` support, present-only filter pills, category-map-derived spec constants, `cover_alt` escape, config comment truth) and test-quality hardening (privacy-scan widening, slug-matched archive spec + `data-slug` attribute, DOM-derived partial-pill helper, fixture decoupling, null-guards, prod env → apex, README rewrite).

## Security assessment

- **Posture strengthened, not weakened:** the email/phone privacy invariant now scans **every** text-like served file (JS, CSS, SVG, webmanifest, sourcemaps, extension-less) instead of four extensions — closing a real gap between the stated promise and its enforcement. `cover_alt` is now escaped (consistency; owner-authored values, no current exploit path).
- **New template output:** `data-slug="{{ include.item.slug }}"` on archive rows — slug values are spec-constrained (`slug_safe` style, lowercase/hyphen) and already appear in URLs; no new exposure. Filter includes render strictly fewer elements (present-only pills).
- **No secrets, no PII, no workflow/CI changes, no new dependencies** (Nokogiri use in batch B rides the existing bundle).
- README rewrite verified free of internal details (no hosts/IPs; references only committed files).

## Findings

No CRITICAL, HIGH, MEDIUM or LOW security findings.

## Severity roll-up

| CRITICAL | HIGH | MEDIUM | LOW | NONE |
| -------- | ---- | ------ | --- | ---- |
| 0        | 0    | 0      | 0   | —    |
