# Security review — PR #36: canonical-host correction (apex)

**Date:** 2026-06-09
**Reviewer:** local code-reviewer subagent (Opus) + security reasoning. The
full security-review skill from PR #35 covers the identical surface; this is
a config/SEO-string follow-up with no new attack surface, so the security
pass is scoped accordingly (documented below).
**Scope:** `_config.yml` (url www→apex + comment), `CNAME` (www→apex),
`.github/workflows/build-and-deploy.yml` (smoke target www→apex),
`_data/schema_person.yml` (Person JSON-LD url www→apex), `smoke_spec.rb`
(assert apex), new `canonical_host_spec.rb` (host guard).

## Security assessment

- **No secrets/tokens** introduced or touched. The only value changes are a
  hostname string (www.delabie.tech → delabie.tech) across config, CNAME, a
  data file, and the deploy smoke target.
- **No new attack surface.** Identical category to PR #35 (reviewed clean):
  static-site canonical-host configuration. No code execution paths, no
  template interpolation of untrusted input, no auth/secrets.
- **Email/phone privacy invariant** untouched (`email_privacy_spec` unchanged
  and still green); `schema_person.yml` still carries no email/phone.
- **New guard strengthens posture:** `canonical_host_spec.rb` fails the build
  if any non-canonical host (incl. preview/localhost) appears in served
  output — a small defense-in-depth gain.
- **Code review (Opus subagent):** found the one real issue (a www URL in
  `schema_person.yml` leaking into served JSON-LD) — fixed in this PR; built
  `_site` now contains zero `www.delabie.tech`.

## Findings

No CRITICAL, HIGH, MEDIUM or LOW security findings. (The CRITICAL the code
review raised was an SEO-correctness leak, not a security vulnerability, and
is fixed here.)

## Severity roll-up

| CRITICAL | HIGH | MEDIUM | LOW | NONE |
| -------- | ---- | ------ | --- | ---- |
| 0        | 0    | 0      | 0   | —    |
