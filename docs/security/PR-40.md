# Security review — PR #40: sitemap fix (code review batch A)

**Date:** 2026-06-10
**Reviewer:** lead-reviewer reasoning (budget mode — no agent ceremony; precedent: PR-36/PR-37). Implementation by an Opus subagent under TDD; independently verified by the lead reviewer (diff read, full suite, built-output inspection).
**Scope:** 2 files — `sitemap.xml` (concat `site.tabs`; lastmod only from explicit frontmatter dates) and `tests/structural/sitemap_spec.rb` (config-driven coverage derivation + new invariants).

## Security assessment

- **No new attack surface.** Build-time Liquid over owner-authored collections; no new interpolation sinks (loc URLs keep the existing `xml_escape`).
- **Information exposure check:** the sitemap now lists the 18 tab pages — all already public, linked from the nav, and indexable since cutover. The `sitemap: false` opt-out (redirect stubs, norobots) is honored and now spec-asserted.
- **No secrets, no PII** (email/phone privacy spec untouched and green).
- The spec additions read `_config.yml` and frontmatter via the existing `YAML.safe_load_file` + `I18nPairs.frontmatter` safe paths (permitted classes unchanged).

## Findings

No CRITICAL, HIGH, MEDIUM or LOW security findings.

## Severity roll-up

| CRITICAL | HIGH | MEDIUM | LOW | NONE |
| -------- | ---- | ------ | --- | ---- |
| 0        | 0    | 0      | 0   | —    |
