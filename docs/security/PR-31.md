# Security review — PR #31: Launch content sprint

**Date:** 2026-06-04
**Reviewer:** local security review (Opus) via general-purpose subagent acting in security role.
**Scope:** 6 commits, 28 files — owner-authored content (About, Contact, CV variants, 3 case-study overviews, activity entries), config/tagline strings, i18n strings, Person-schema strings, a new hero block in `_layouts/home.html`, Playwright tests.

## (1) What was reviewed

- New hero block Liquid interpolation (`_layouts/home.html`) — i18n strings into HTML, `relative_url` on hrefs.
- New `description:` frontmatter flowing into meta tags via jekyll-seo-tag.
- `_data/schema_person.yml` string changes flowing into Person JSON-LD.
- Email/phone privacy invariant across all changed content.
- External links and secrets scan over the full diff.

## (2) Security-relevant decisions

- Hero strings are static owner-authored literals in `_data/i18n.yml`; no untrusted-input boundary exists at build time.
- Meta descriptions are escaped by jekyll-seo-tag; JSON-LD values pass through `jsonify`.
- Contact pages remain email/phone-free (LinkedIn primary); `email_privacy_spec.rb` continues to enforce the invariant over built output.

## (3) Findings

No CRITICAL, HIGH, MEDIUM or LOW findings.

## (4) Risks accepted with rationale

| Tag | Severity | Risk | Rationale |
|---|---|---|---|
| — | — | None | Clean content PR under the documented threat model (solo-maintained static site, owner-authored content, build-time Liquid, no untrusted input) |

## (5) Recommended follow-ups

- None blocking.

## Severity roll-up

| CRITICAL | HIGH | MEDIUM | LOW | NONE |
| -------- | ---- | ------ | --- | ---- |
| 0        | 0    | 0      | 0   | —    |
