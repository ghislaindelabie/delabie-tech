# Security review — PR #35: Phase 8 production cutover

**Date:** 2026-06-08
**Reviewer:** local security review (Opus) via general-purpose subagent acting in security role.
**Scope:** config-only go-live — `_config.yml` (url → https://www.delabie.tech, robots_noindex → false, goatcounter.code set, header comment), `CNAME` → www.delabie.tech, the post-deploy smoke target → www, `.gitignore` (+`.smbdelete*`), and two preview-coupled tests flipped to production invariants.

## (1) What was reviewed

- Whether the GoatCounter count URL is a secret (it is not — public ingestion endpoint, shipped client-side by design).
- Whether flipping `robots_noindex` off newly exposes anything confidential (no — site-wide preview precaution; no drafts / `published:false` / private pages; the email/phone privacy invariant via `email_privacy_spec` is untouched and still enforced).
- url/CNAME canonical-host change (no security impact; the new smoke spec tightens `url == https://<CNAME>`).
- Any secret/token in the diff (none); `google_site_verification` blank emits nothing.

## (2) Findings

No CRITICAL, HIGH, MEDIUM or LOW findings.

## (3) Risks accepted with rationale

| Tag | Severity | Risk | Rationale |
|---|---|---|---|
| — | — | None | Config-only cutover; only already-public personal-site content becomes indexable; no secrets; privacy invariant intact |

## Severity roll-up

| CRITICAL | HIGH | MEDIUM | LOW | NONE |
| -------- | ---- | ------ | --- | ---- |
| 0        | 0    | 0      | 0   | —    |
