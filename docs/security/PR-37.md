# Security review — PR #37: archive additions (RFI 2017) + 2017 unlinks

**Date:** 2026-06-09
**Reviewer:** local code-reviewer subagent (Opus) + security reasoning. Content-only
PR (archive markdown frontmatter); no code, no templates, no config, no
workflows — nil new attack surface, so the security pass is scoped to that
reasoning (consistent with PR #36's content/config follow-up record).
**Scope:** new `_archive/rfi-waze-smart-city-2017.{md,fr.md}`; `projects: [maas-standards]` → `projects: []` on `_archive/medium-marin-autonomy-paris-2017.{md,fr.md}`.

## Security assessment

- **No code / templates / config / workflows touched** — four Markdown content files only. No execution paths, no interpolation logic changes.
- **No secrets/tokens.** `original_url` is a public RFI article URL.
- **No PII introduced.** No email/phone; `email_privacy_spec` unchanged and green.
- **Canonical-host guard green** (`canonical_host_spec`): built stubs use the apex; `www.rfi.fr` in `original_url` is an external link, not the site host, and is the intended outbound target.
- **JSON-LD** for the new item is owner-authored frontmatter rendered through the existing (jsonify-escaped) archive-jsonld include — same safe path as every other archive item.

## Findings

No CRITICAL, HIGH, MEDIUM or LOW security findings. (Code review raised only a
source-fact-check SUGGESTION — satisfied: the entry's facts come verbatim from
the article text the owner supplied — and two pre-existing LOW template notes.)

## Severity roll-up

| CRITICAL | HIGH | MEDIUM | LOW | NONE |
| -------- | ---- | ------ | --- | ---- |
| 0        | 0    | 0      | 0   | —    |
